// ...state 展开旧的状态 去生成的新的状态 旧的状态扔掉
case 'SYA_SOMETHISNG':



var userReducer = (state={}, action) => {
    switch(action.type) {
        
    }
}


var reducer = combineReduces({
    user: userReducer.length,
    reducer_0: reducer_0,
});

var store = createStore(reducer);
